version: '3.8'

services:
  mc_menu:
    container_name: mc_menu_0
    build: .
    ports:
      - "3080:3008"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - PORT=3008
      - MONGO_URI=mongodb://mongo:27017/kittydelivery
    networks:
      - shared-net

networks:
  shared-net:
    external: true
